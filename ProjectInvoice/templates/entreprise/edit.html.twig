{% extends 'base.html.twig' %}

{% block title %}Modifier l'Entreprise{% endblock %}

{% block body %}
    <div class="container mt-4">
        <div class="card shadow-sm p-4">
            <h1 class="mb-4 text-primary">Modifier l'Entreprise</h1>

            {{ include('entreprise/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            <div class="d-flex justify-content-between mt-3">
                <!-- <a href="{{ path('app_entreprise_index') }}" class="btn btn-secondary">Retour à la liste</a> -->
                {{ include('entreprise/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}