{{ form_start(form) }}
    <div class="mb-3">
        {{ form_widget(form) }}
    </div>
    <button class="btn btn-primary" type="submit">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}